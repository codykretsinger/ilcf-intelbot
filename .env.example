# ============================================================
# IntelBot v2.8 Configuration
# ============================================================

# ------------------------------------------------------------
# SLACK CREDENTIALS (Required)
# ------------------------------------------------------------
# Get these from: https://api.slack.com/apps
# Socket Mode must be enabled for SLACK_APP_TOKEN

SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_APP_TOKEN=xapp-your-app-token-here

# ------------------------------------------------------------
# API KEYS (Optional but Recommended)
# ------------------------------------------------------------
# All services have free tiers - sign up at respective websites
# Bot will skip APIs if keys are not provided

# VirusTotal (File/URL/Hash reputation)
# Free tier: 500 requests/day, 4 requests/minute
# Get key: https://www.virustotal.com/gui/my-apikey
VT_API_KEY=your_virustotal_key_here

# AbuseIPDB (IP reputation)
# Free tier: 1000 requests/day
# Get key: https://www.abuseipdb.com/account/api
ABUSE_IPDB_KEY=your_abuseipdb_key_here

# Shodan (Infrastructure scanning)
# Free tier: 100 requests/month
# Get key: https://account.shodan.io/
SHODAN_API_KEY=your_shodan_key_here

# URLScan.io (URL sandboxing)
# Free tier: 50 requests/day
# Get key: https://urlscan.io/user/profile/
URLSCAN_API_KEY=your_urlscan_key_here

# ------------------------------------------------------------
# IOC MANAGEMENT
# ------------------------------------------------------------
# Path where IOC files will be stored (container path if using Docker/Podman)
IOC_FILE_PATH=/data/iocs.txt

# Public URL where IOC files are accessible (optional - for !list command)
# Leave blank if not serving publicly
IOC_PUBLIC_URL=https://your-domain.com/intel/iocs.txt

# ------------------------------------------------------------
# CACHING (Optional - Performance Tuning)
# ------------------------------------------------------------
# Response caching reduces API calls and improves performance
CACHE_ENABLED=true
CACHE_TTL=300                # Cache lifetime in seconds (default: 5 minutes)
CACHE_MAX_SIZE=500           # Maximum cache entries

# ------------------------------------------------------------
# THREAT DETECTION THRESHOLDS (Optional)
# ------------------------------------------------------------
# Customize severity scoring sensitivity

# AbuseIPDB thresholds
THRESHOLD_ABUSE_CRITICAL=80       # Critical threat score threshold
THRESHOLD_ABUSE_SUSPICIOUS=20     # Suspicious activity threshold

# Domain age thresholds (in days)
THRESHOLD_DOMAIN_NEW_DAYS=30      # Flag domains newer than this
THRESHOLD_DOMAIN_CRITICAL_DAYS=2  # Critical: newly registered domains

# ------------------------------------------------------------
# API RATE LIMITS (Optional - Only for Paid Tiers)
# ------------------------------------------------------------
# Set these ONLY if you have paid API plans
# Defaults use free-tier limits if not specified

# VT_DAILY_LIMIT=500
# VT_MINUTE_LIMIT=4
# ABUSEIPDB_DAILY_LIMIT=1000
# SHODAN_MONTHLY_LIMIT=100
# URLSCAN_DAILY_LIMIT=50

# ------------------------------------------------------------
# LOGGING (Optional)
# ------------------------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Log directory (default: logs/)
LOG_DIR=logs

# ------------------------------------------------------------
# ADVANCED SETTINGS (Optional)
# ------------------------------------------------------------
# API request timeouts (in seconds)
API_TIMEOUT=10               # General API timeout
URL_EXPAND_TIMEOUT=5         # URL expansion timeout
